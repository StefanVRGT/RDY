# Database Configuration
DATABASE_URL=postgresql://rdy:rdy_dev_password@localhost:5434/rdy

# PostgreSQL Docker Configuration (optional, defaults are used if not set)
POSTGRES_USER=rdy
POSTGRES_PASSWORD=rdy_dev_password
POSTGRES_DB=rdy
